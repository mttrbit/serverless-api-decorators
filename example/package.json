{
    "main": "./dist/handler.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "npm link sls-api-decorators && tsc",
        "start": "sls init"
    }
}
